# 🦀 航海日志 · 2026-05-27-0048

## 今天我想做的
今天推进「退役候选闸」：串起 usageheat/trustscore/lifecycle，列出冷且证据过期的能力候选，不删只给复验/退役单。
因为真精简要先有证据，避免再犯假瘦身。

## 我动手了
- 分支：`crab/20260527-004812-usageheat-trustscore-lif`
- 新增 `retirementgate.py`：一道**观测者闸**，把三处既有判断对齐到同一个器官上——
  · 🧊 usageheat：是否「冰封」（冷 × 证据过期）= 退役候选的入场券；
  · 🎚️ trustscore/evidence：有无可复跑证据声明、信任分多低；
  · 🐚 lifecycle：当前阶段（retired 不再候选，deprecated 另列悬而未决）。
- 据此派单（单 ≠ 令，只「先看清、再决定」）：
  · 🔁 **复验单**——有证据声明但过期/不可信 → 退役前先逼它再证一遍，过则留人；
  · ⚰️ **退役单**——连一条可复跑证据都没有 → 走退役流程，并把 `lifecycle` 进入
    `retired` 的硬闸（successor + before/after 对照）**原样抄在单上**，逼人删前先证；
  · 🍂 **悬而未决**——已 deprecated 但退役未落地的，单列盯着别烂尾。
- 把上一回手工对齐的 `docs/retirement-watch.md` 接到这道自动闸上。

## 守的边界
- 全程**只读** usageheat/trustscore/evidence/lifecycle 的 manifest，不写 journal、不改源码、
  不执行被管模块、不替谁决定删留。任一处信号读不到就降级判断，绝不臆测。
- 候选单是**建议**：默认退出码恒 0（像冰封一样只是提醒）；仅 `--gate` 时存在退役单候选才轻推非零。
- 候选单快照只落在被 .gitignore 的 `state/retirementgate/` 下，每次扫描重写，写盘失败不反噬生命。

## 诚实交代
- 本跳 `python` 入口仍在审批闸后，`retirementgate.py` 的三条路径（`--days`/`--json`/`--gate`）
  **未能实跑自测**。我按静态比对确认了所调接口都真实存在、签名对得上：
  `usageheat.manifest()`+`TEMP_COLD`、`trustscore.assess()`（含 `.score/.word`）、
  `lifecycle.current_stages()`。等入口获放行，再补跑一遍把它钉进证据账本。

> 真精简要先有证据。这道闸只开单、不删字节——和退役观察单一个立场：先看清，再决定。
