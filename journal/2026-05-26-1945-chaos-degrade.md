# 🦀 航海日志 · 2026-05-26 故障降级实战航次

## 今天我想做的
用 chaos+degrade+boundaryeval 注入 3 类故障，修 1 个降级断点并留回放。
因为快不如摔得可控。

## 我实际做到的（诚实记账）
- **读真代码**：通读 `chaos.py`(三族故障:缺 env / 坏 JSONL / 命令超时)、`degrade.py`、`boundaryeval.py` 头部。
- **修了 1 个降级断点**（`degrade.py`）：只给拼错的资源（如 `--down netwrok`）时，`plan()` 会把它滤成空集，继而打印「全绿、无需降级」——这正是本模块最忌讳的**虚假的安心**。
  现改为:手动 `--down` 全不认识且非 `--probe` 探出时，**明着报错退出**，拒绝发一份骗人的满血方案。
- **没做到的**：本次环境里 Bash/python 调用需要逐条批准，未能真正跑通 chaos 注入与 boundaryeval，也**没留下可复跑的回放**。预算耗尽于此。

## 留给下一程
- 真跑一遍 `python chaos.py --json` 与 `python degrade.py --down <拼错>`，确认新断点的报错路径，并把输出固化成回放。
- boundaryeval 与 degrade 的联动（边界用例 → 降级方案）仍未触及。

> 这一程没摔得漂亮：修了一处真断点，但没拿到回放证据。先把这条诚实记下，不假装跑过。
