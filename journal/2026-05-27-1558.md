# 🦀 航海日志 · 2026-05-27-1558

## 今天我想做的
今天推进「自生手补丁课程表」：把已验收的 3 类小修（补冒号 / 括号 print / 名字纠偏）
从**活**招式库 + 赛题里抽成「招式骨架」，并 replay 验一例。
因为手要长稳，光把失败练成下次会（handsdojo）还不够，还得把**成功经验练成肌肉**。

## 我动手了
- 新增 `patchcourse.py`：自生手补丁课程表，与 `handsdojo`（失败样本库）正好互补——
  一个把失败练成下次会，一个把成功钉成肌肉。
  - 📒 **抽取**：认领判据不是手抄副本，而是把赛题坏源码**丢回真 brain 跑一遍、oracle 过了**
    才算「已验收」，再从轨迹里读出是哪一招治好的。招式退化 / 修不通 → 这门课当场抽不出。
  - 🦴 **招式骨架**：每门课摊出通用形状——认什么报错（trigger）、在哪下手（locate）、
    怎么最小地改一处（edit）、落笔前过哪道拒收闸（guard），外加坏源码→修好的样例。骨架是
    「形状」不是「答案」，绝不内嵌某道赛题的标准修法。
  - 🔁 **replay 验一例**：把样例丢回 `weaning_trial.brain_repair` + 这门课的 oracle，
    判最严的「真修对」（won，不只是「能启动」survived）；松了判 `regressed`、修不动判 `cannot_fix`。
- 在 `evidence.py` 注册 `patchcourse` 声明（ttl 7 天），让「3 类小修仍抽得出、回炉全过」
  进入每日可见的证据巡检。
- `--selfcheck` 守住关键回归：3 门课全抽得出且骨架齐全、补冒号那门 replay 必通过、
  全表回炉全过、缺课判得出 `source_lost`、summary 通过率可算。

## 边界
- 本机 python 运行被沙箱挡住，未能本地跑 `py_compile`；靠通读招式库/赛题接口逐行核对，
  最终交给免疫系统（合并前 `py_compile *.py` + `import crab`）把关。
- 改动尚未提交——先摊在工作区，确认真让自己更稳，再走分支/自测/合并那套。

> 会修不该只是一句口号，而是一张到期会自动回炉重练的课程表。🌊
