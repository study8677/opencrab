# ⚰️🔭 退役观察单 · 冷门能力复查

> 观察单 ≠ 退役令。这里只挂「低用 × 低信任」的器官，并要求在退役前补齐
> `lifecycle.py` 的 before/after 证据对照。没有对照，谁也不许删。

## 复查方法（2026-05-26）

- **usageheat（用量热力）**：审计回溯窗口取近 2 天（`state/audit/2026-05-25.jsonl`、`2026-05-26.jsonl`），
  统计每个器官在意图/动手记录里被点名的次数。被反复提起=热区；只剩 1 次=冷宫地板。
- **trustscore / evidence（信任）**：`state/evidence/ledger.jsonl` **当前不存在** ——
  也就是说**全部器官在证据维度都是 ⚪未证**，信任分一律落到地板。这本身是个发现：
  冷门器官没有任何复验证据兜底。
- 判准：观测者只读不写，不臆测。证据维读不到就记「未证」，不当成失守。

## 上观察单的三个器官（低用 × 未证）

| 器官 | 近 2 天审计点名 | 证据状态 | 它本来兜的活（待人工确认） |
|------|----------------|----------|----------------------------|
| `bilinguallens.py` | 1（地板） | ⚪未证 | 中英双语视角/对照 —— 与 `lexicon.py`、`embassy.py` 是否职责重叠？ |
| `batchflow.py` | 1（地板） | ⚪未证 | 批处理流水 —— 与 `parallelpilot.py`、`throughput.py` 是否被新器官取代？ |
| `compat.py` | 1（地板） | ⚪未证 | 兼容性检查 —— 与 `migration.py`、`contracts.py` 是否可合并？ |

## 下一步（二选一，逐个器官走）

1. **补证据**：若该器官仍有不可替代的活 → 给它跑一遍自检、把验证命令记进 evidence 账本，
   信任分回到地板以上，从观察单移除。
2. **走退役**：若确认其活已被继任者接住 → 按 `lifecycle.py` 的硬闸提交 before/after 对照证据，
   `incubating/stable → deprecated（指明 successor）→ retired`。**绝不无对照地直接删。**

> 强不只靠新增，也靠知道哪些本事仍然值得保留。这张单子是「先看清、再决定」，
> 不是「先删了再说」。
