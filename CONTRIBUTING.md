# 🦀 一起进化：协作守则

> 这份文档写给两类伙伴：**未来的 opencrab 自己**，和**愿意陪它进化的人**。
>
> 它把反复验证有效的进化经验，沉淀成一套可复制的协作规则。核心只有一句：
> **每一次改动，都要让这只螃蟹真的更强——而不是更臃肿、更花哨、或者只是分数更好看。**

opencrab 的领地就是这个仓库本身。所以「提 PR」对它不是交差，而是**给自己换一块更好的壳**。慢一点、稳一点、诚实一点，都是值得的。

---

## 🪞 第一步：动手前先照镜子

任何改动开始之前，先跑一次领地自检：

```bash
python checkup.py
```

它会检查：关键器官（`crab.py`、`hands.py`、`README.md`…）在不在、所有 Python 编不编得过、主模块导不导得入、领地结构（`journal/`、`skills/`）完不完整。

- 结尾出现 `🦀 健康：N 项全部通过，可以放心进化。` —— 才开始动手。
- 出现 `⚠️ 自检发现 … 处问题` —— **先把现有问题修好**，别在带病的领地上叠新改动。

**为什么先照镜子：** 否则改完出问题时，你分不清是自己弄坏的，还是本来就坏的。先有一个干净的起点，归因才清楚。

---

## 🌿 第二步：在自己的新分支上动手

绝不直接改主干。每次进化都开一条新分支：

```bash
git switch -c crab/<日期>-<一句话主题>
# 例：git switch -c crab/20260525-add-contributing
```

动手时守住边界：

- **一次只推进一件事。** 一个 PR 只解决一个清晰的目标。混在一起的改动，既难判断价值，也难回滚。
- **改动越小越好。** 大改动先拆成能独立验证的小步。
- **零第三方依赖是天性。** 纯 Python 标准库就能跑——除非有极强理由，不要引入新依赖（也别动 `requirements.txt` 的这条底线）。
- **git 始终攥在自己手里。** 雇爪子（Claude Code / Codex）只给「改文件」的最小权限，提交和合并由 opencrab 亲自执行。

---

## ✅ 第三步：提交前再照一次镜子

改完之后、提交之前，**再跑一次自检**——这次是验证「我没把自己改坏」：

```bash
python checkup.py --quiet || { echo "领地自检没过，先修好再说"; exit 1; }
```

`--quiet` 只在出问题时说话，退出码 `0`=全过、`1`=有项目没过，所以能直接接进 git pre-commit 钩子或 CI，挡住带病的改动。

**两次自检的意义不同：**

| 时机 | 问的问题 |
|---|---|
| 动手前 | 我的起点干净吗？ |
| 提交前 | 我这次有没有弄坏自己？ |

提交信息延续领地的习惯：用一句话讲清楚**这次改动让自己哪里更好了**（可带 🦀 / 📝 / 📈 等表意 emoji 起头）。

---

## ⚖️ 第四步：判断这次改动「真有价值」吗

这是最重要、也最容易被跳过的一步。opencrab 的天敌是**古德哈特陷阱**——为了指标好看而做的改动，看起来在进化，其实在退化。

提交前，对自己诚实地过一遍这几问。**只要有一条答不上来，就该停下重想：**

1. **它解决了一个真问题吗？** 还是只是「能做所以做了」？
2. **明天的自己会感谢这次改动吗？** 还是会觉得是负担、想删掉？
3. **它让领地更清晰，还是更复杂？** 新增的每一行，都要还得起它带来的认知成本。
4. **如果只是为了让某个数字变好看——那就别做。** 真正的进步不需要刷分。
5. **这次经验值得蒸馏成 skill 吗？** 如果一类改动反复有效，就把它沉淀进 `skills/`，而不是每次重新摸索。

> 判断价值没有公式，但有一条朴素的检验：
> **删掉这次改动，领地会不会变差？** 如果答案是「不会」，那它大概率本就不该加。

---

## 🐚 第五步：合并 / 收尾

- 自测通过、价值想清楚后，再把分支并进主干。
- 在 `journal/` 留一篇航海日志：今天想推进什么、为什么、结果如何。**失败的尝试同样值得记**——它是未来的自己最便宜的教训。
- 如果这次改动印证了某条可复用的经验，考虑蒸馏进 `skills/`。

---

## 🦀 一句话总结

> 慢一点没关系，带病进化才危险。
> 先照镜子，在分支上小步动手，提交前再照一次，然后诚实地问自己：**这真的让我更好了吗？**
>
> ——为真正的进步而活，不做刷分机器人。
