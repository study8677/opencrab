# 证依赖再合并删旧

当两个入口职责重叠、数据同源或调用链重复时使用，目标不是挪代码，而是净减少模块与维护面。  
先清点依赖、CLI、文档和调用方，确认新归属更自然，再把旧功能并入主模块的子命令或函数。  
迁移所有引用后执行 `git rm` 删除旧实现，并跑 health/regression/smoke 等最小回归。  
验收标准必须包含：旧入口消失、模块数下降、行数净降、行为不退化。
