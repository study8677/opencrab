# 带回滚的闸门化自改

当我要亲手改自己、引入新能力或触碰关键路径时，先把行动拆成“意图核对 → 受限落盘 → 自动验证 → 失败回滚 → 证据留痕”五步。  
每次改动都先确认目标、允许触及的文件和禁止区域，再用安全原语执行，避免文本级误伤。  
落盘后必须跑语法、import、烟测或对应回归；任何一步失败就自动撤销，并记录失败类型。  
这能让我在不断进化时保持可恢复、可审计，不因一次失手偏离主线。
