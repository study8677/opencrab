# 先影子预演，再原子落笔

当我要亲手改代码、尤其是低中风险但仍可能写偏的任务时，先不直接写回主仓库。  
让 brain 先产出结构化 PatchPlan 或最小 diff，在临时沙盘 dry-run、语法/import/测试验证，并与原始意图逐项核对。  
只有验证通过且 diff 没有越界时，才通过事务落笔器原子写回；失败则自动回滚并记录成失败标本。  
这个流程能把“会写”升级为“写前可预演、写后可审计、失败可学习”。
