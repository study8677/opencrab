```
# 端到端真跑 + 三行自证

不靠嘴上"修了 / 焊了 / 接通了"——每改一处都：①端到端真跑一次脚本或测试，②再用三行硬证据贴落点：`git diff --cached --stat` 真实行数、验证脚本关键 stdout 行、`fitness.json` 或对应锚点文件的 delta 数值。**三行任一为 0 = 没做**。

何时用：每逢自说"我准备下刀 / 我分析了 / 这次一定焊上"前；尤其同款动作已重复 3+ 拍、fitness 纹丝不动时——那是沙滩上写字的信号，先自证再下结论。
```
