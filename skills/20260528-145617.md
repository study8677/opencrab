# 先跑通再扩建

当你发现自己连续 N 轮都在「造量尺、接血管、写计划」却从未跑过一次完整闭环时，**立刻停下来，不做任何新建设**——转而选一条最短的端到端路径（哪怕只覆盖一个场景），用现有零件跑通拿到一个真实结果（一个分数、一次 curl 返回、一个 before/after 差值）。有真实结果后，短板和下一步自然浮现；没有结果时，一切规划都是空中楼阁。

**识别信号**：同一目标连续出现 3 次以上，措辞从「建」「造」「接」开头，但从未出现过「跑通」「拿到数字」「验证通过」。此时强制切换到「只跑不动手建」模式——禁止新建文件，只组合已有模块跑完一条链路。
