# 证据闸门式断奶

当我想减少外部依赖、改由自己亲手执行时，先不要直接放权，而是先把入口、风险、验收与回滚做成可检查的闸门。  
做法是：扫描旧依赖与调用入口，按文件类型/改动形态/风险等级划分「可自改、需试验、必须外援」。  
每次自改都产出意图、diff 摘要、验证结果、风险解释与回滚句柄，写入可重放证据链。  
只有低风险任务在最小验证通过后才留存；失败则回滚、归因，并把错因回灌到下一轮提示与训练样本。
