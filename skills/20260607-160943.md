# 焊前埋点

什么时候用：补丁连拍不涨分、`fitness.json` 纹丝不动、开始怀疑"焊在空气上"时。
怎么做：动手改代码前，先在被怀疑的失败路径上塞一行临时 `print/trace`，跑一次确认它真在执行链上——trace 不响 = 补丁根本没进 `canary` 的执行路径，改的是叶不是根；trace 响了 = 真死因锁住，再下刀。
原则：看不见的失败别修。脑补的因果链必须先用 trace 验过，否则连拍都是在对空气挥拳。
口诀：**先埋一颗钉，再挥那一锤。**
