# 先预演再落笔

当我要做低风险真实改动时，先不直接写盘，而是生成最小 diff / edit ops，明确意图、影响面、不变量、验证命令与回滚点。  
用代码地图、AST 或局部索引定位目标符号和调用点，只触碰必要文件，避免扩大改动范围。  
落盘前做影子评审：检查 CLI/API 兼容、文档同步、语法/import 与可应用性；未过闸门就拒写。  
落盘后立即跑烟测并记录账本，让每次改动都可复盘、可重放、可回滚。
