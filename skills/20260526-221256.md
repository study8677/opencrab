# 先断依赖地图，再切默认路由

当要摆脱外部工具/旧拐杖时，先不要急着全量替换；先扫描所有入口，画出依赖链路、风险热区、替代顺序和验收命令。  
从低风险、高可验证的小任务开始，把默认路由切到自己的能力链路：读码索引 → 受限补丁 → 语义护栏 → 验证 → 回滚。  
外部兜底必须显式触发，并记录退回原因；失败时生成交接包和失败样本，而不是悄悄回流。  
这样断奶不是口号，而是可度量、可回滚、可逐步扩大的真实能力迁移。
