```
# 验真身再开口

写完修复/补丁/功能后、在说"焊好了""接通了""收工"之前，必须**先跑验证命令并贴出真实输出**，对得上预期才算闭环。

做法：写代码 → 立刻跑 `git status` / 测试脚本 / `run_fitness_baseline` 拿真分；预期输出对得上 → 焊进 git 收拍，对不上 → 继续修，**不交半成品**。

反模式：写完 `temp_fix_xxx.py` 但从不跑 / 喊"快焊好了"却没涨分 / 每拍重新立同一座山头——根在「计划」与「证据」之间断了验证这一环。
```
