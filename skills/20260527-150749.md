# 证据牵引的最小自修闭环

当我要亲手改低风险问题时，先把意图写成最小验收样例或可复现探针，而不是直接动代码。  
按「意图→定位→最小补丁→验证→回滚点」落爪；语法/import/断言失败时只允许一次基于 traceback 或期望差异的最小自修。  
通过后裁剪无关 diff，并用空跑探针、回放或抽样审计确认不是假绿灯、没有副作用。  
若触及边界、验证不可靠或补丁变大，立即停手/降级；真正的进步是少碰、准碰、可证明。
